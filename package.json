{
  "name": "himbaza-imana-cloudflare",
  "private": true,
  "scripts": {
    "build:cloudflare": "node tool/build_cloudflare.mjs",
    "deploy:cloudflare": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
